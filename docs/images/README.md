# Wiki screenshots

Drop PNG screenshots here with the **exact filenames** below — the wiki pages
already link to them. Until a file exists, its link renders as a broken-image icon
(harmless).

Keep images reasonably sized (≤ ~1600 px wide, PNG). On Windows, `Win+Shift+S`
captures a region; save it here with the matching name.

| Filename | Page | Should show |
|----------|------|-------------|
| `pipeline-app-launch.png` | Quickstart | napari window with the LimonCELLo dock on first launch |
| `quickstart-classified.png` | Quickstart | cilia coloured by `log_ratio` after Assign & classify |
| `pipeline-app-annotated.png` | Pipeline app | full panel with ①–⑤ regions marked (add arrows/numbers) |
| `batch-section.png` | Batch | the Batch section with its checkboxes + the two run buttons |
| `annotator-app.png` | Annotator | boxes drawn around cilia on the XY MIP, with class tags |
| `dataapp-overview.png` | Data app / Quickstart | the Streamlit dashboard with a run loaded |
| `dataapp-day-coverslip.png` | Data app | Distribution tab, Compare by = day, coloured by coverslip |
| `training-section.png` | Training APOC | the 🎓 training section (annotate / features / model) |
| `theory-iso-mip.png` | Theory | isotropic vs MIP illustration (or a real before/after) |
| `theory-ratio-map.png` | Theory | the log-ratio map overlaid on a sample |
| `theory-pairing.png` | Theory | cilium→basal-body links (nearest vs axis-directed) |

> Some theory figures can be generated from `media/explainers/` (manim scenes) if
> you prefer rendered diagrams over screenshots.

Existing figures already referenced from `../assets/` (no need to recreate):
`image.png`, `cilialpha3_learning_curve.png`, `cilialpha3_roc.png`,
`analysis-complete.png`.
